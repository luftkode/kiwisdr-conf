{
  "name": "kiwisdr-conf",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "watch": "tsc -w",
    "minify": "mkdir -p html && for f in typescript_dist/*.js; do npx terser \"$f\" --compress passes=2,toplevel=true --mangle toplevel=true -o \"html/$(basename \"$f\")\"; done",
    "release": "npm run build && npm run minify"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Ultraegern/kiwisdr-conf.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Ultraegern/kiwisdr-conf/issues"
  },
  "homepage": "https://github.com/Ultraegern/kiwisdr-conf#readme",
  "devDependencies": {
    "terser": "^5.44.0",
    "typescript": "^5.9.3"
  }
}
